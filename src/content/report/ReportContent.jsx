// The canonical App imports both entrypoints. This dashboard-only project has
// no authored report; retain the export without bundling the gallery narrative.
export function ReportContent() { return null; }
